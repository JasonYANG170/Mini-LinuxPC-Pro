#!/bin/sh
set -e
# Automatically added by dh_installinit/13.5.2ubuntu1
if [ -z "${DPKG_ROOT:-}" ] && [ "$1" = remove ] && [ -x "/etc/init.d/udhcpd" ] ; then
	invoke-rc.d udhcpd stop || exit 1
fi
# End automatically added section
